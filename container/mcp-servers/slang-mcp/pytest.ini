[pytest]
testpaths = test
python_files = test_*.py
addopts = -v
asyncio_default_fixture_loop_scope = function
markers =
    asyncio: mark test as asyncio
    integration: mark test as integration (calls real APIs, requires tokens)
